//! `playtest` binary: subcommand dispatch for `play`, `replay`, and `report`.

fn main() {
    println!(
        "playtester {} — subcommands not yet implemented",
        env!("CARGO_PKG_VERSION")
    );
}
